package com.power.ssm.dao;

import com.power.ssm.common.Dao;
import com.power.ssm.model.Calculate;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface CalculateDao extends Dao<Calculate> {
}
